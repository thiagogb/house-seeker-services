query  {
    providers {
        rows {
            id
            name
            active
        }
        pagination {
            pageNumber
            pageSize
            totalPages
        }
    }
}